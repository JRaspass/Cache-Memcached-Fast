TYPEMAP
Cache_MemcachedFast *      T_CACHE_MEMCACHED


INPUT
T_CACHE_MEMCACHED
        if (sv_derived_from($arg, \"Cache::MemcachedFast\"))
          {
            IV tmp = SvIV((SV*) SvRV($arg));
            $var = INT2PTR(Cache_MemcachedFast *, tmp);
          }
        else
          croak(\"memd is not of type Cache::MemcachedFast\");


OUTPUT
T_CACHE_MEMCACHED
        sv_setref_pv($arg, class, (void*) $var);
